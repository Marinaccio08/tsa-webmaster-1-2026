# tsa-webmaster-1-2026

Install node and npm
Run `npm install` in the terminal to set up

Run `npm run dev` to test website

Save html files to the `views` folder.

- use [Nunjucks](https://mozilla.github.io/nunjucks/getting-started.html) for html

Put all CSS and images in `public` folder (can make subfolder for images)
